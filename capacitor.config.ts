import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ai-bot-pocket-option',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

